if ((location.pathname == '/books') || (location.pathname.indexOf('users')) == 1) {
  $(".book_<%= @book.id %>").html("<%= j(render 'books/book',  book: @book ) %>")
} else {
  $(".book_<%= @book.id %>").html("<%= j(render 'books/detail',  book: @book ) %>")
}
